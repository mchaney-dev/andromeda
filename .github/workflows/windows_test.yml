name: windows

on:
  push:
    branches:
      - main
  
jobs:
  test:
    name: windows
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["windows-latest"]
        python-version: ["3.8"]
    steps:
      - name: Install packages
        run: |
          pip install andromeda-nlp==0.1.3
          pip install pytest
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Test package
        run: |
          python ./tests/test_release.py
